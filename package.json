{
  "name": "desihippe",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "process": "node scripts/process-content.mjs",
    "dev": "node scripts/process-content.mjs && vite",
    "build": "node scripts/process-content.mjs && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright": "^1.61.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "marked": "^18.0.5"
  }
}
